import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(); // Background color for the entire app
  static const Color backgroundElements = Color(); // Background color for elements like buttons, cards, etc.
  static const Color accent = Color(); // Accent color for buttons, icons, etc.
  static const Color textPrimary = Color(); // Primary text color for the app
  static const Color textSecondary = Color(); // Secondary text color for the app

}